<h2>User details</h2>

<dl class="dl-horizontal">
  <dt>Full name</dt><dd><%= @user.full_name%></dd>
  <dt>Email</dt><dd><%= @user.email%></dd>
</dl>

<% if @user_stocks.size > 0%>
  <%= render 'stocks/list' %>
<% else %>
  <span>No stocks followed</span>
<% end %>
